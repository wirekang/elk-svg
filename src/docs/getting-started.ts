/**
 * # Documentation work in progress.
 *
 * ## Getting started
 *
 * ```
 * npm install elk-svg
 * ```
 *
 * ```
 * import { initElkSvg } from "elk-svg";
 *
 * const container = document.querySelector("svg")
 * const elkSvg = initElkSvg({ container });
 * ```
 *
 * Initialize {@link References.ElkSvg | ElkSvg} instance by calling {@link References.initElkSvg | initElkSvg} function with {@link References.ElkSvgOptions | ElkSvgOptions}.
 * The only required property is `container`.
 *
 * @module Getting Started
 */
